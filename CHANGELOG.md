# Changelog

All notable changes to this project will be documented here.

## [Unreleased]

### Added

- Initial monorepo layout for `@thinglinks/node-red-common@0.1.0`.
- Initial Edge package `@thinglinks/edge-nodes@1.0.1` with `tl-device`, `tl-tag`, and `tl-uplink`.
- Initial private Cloud skeleton `@thinglinks/cloud-nodes@0.1.0`.

These are initial repository/package entries only. They do not indicate an npm publication; the `@thinglinks` scope publishing permission remains unconfirmed.
